# Live Mining Lifecycle and Notification Polling

## Milestone Status

The first opt-in bounded live Stratum mining command is complete. It performs
one caller-bounded nonce range and can submit at most one qualifying result.
It does not continue into another range automatically.

Timeout-aware Stratum notification polling is complete. Its boundary lets an
authorized client return queued notifications immediately or wait for a
caller-bounded duration without treating a normal timeout as a connection
failure. Sanitized structured event logging and native log analysis are now
implemented. Bounded chunk orchestration and the controlled continuous
lifecycle are complete.

## First Live Benchmark

- Hashes checked: 100000
- Elapsed time: 186640333 ns
- Hash rate: 535789.87 H/s
- Result: no qualifying share

This records one bounded run only. It contains no endpoint credentials, payout
address, complete username, password, job payload, or session-specific nonce
material.

## Next Step

Add duplicate-safe search-space expansion through `extra_nonce_2` progression
and network-time rolling before reconnect and recovery behavior.

## Live Transport Regression

- Handshake: succeeded
- Final client state: `AUTHORIZED`
- Notification observation: succeeded
- Arrival order: `mining.set_difficulty`, then `mining.notify`
- Difficulty: 10000
- Mining and share submission: not performed

This regression record intentionally omits the subscription extra nonce,
payout address, complete username, session nonce values, and raw mining job.

## Live JSONL Smoke Test

- Two successful logged handshake invocations appended six total records.
- Each run contained three events.
- Separate run IDs were observed without recording their values.
- Sequence restarted at `1` for each run.
- Event order was `command_started`, `stratum_authorized`, then
  `command_completed`.
- Exact forbidden-field key validation found no secret-bearing fields.
- `extra_nonce_2_size` was present only as allowed safe metadata; it does not
  contain `extra_nonce_2`.

This record intentionally omits actual run IDs, both extra nonce values,
username and payout data, and raw JSON records.

## Logged Bounded Mining and Analysis Validation

- One logged bounded mining run checked 100000 hashes in 173113459 ns.
- The measured rate was 577655.84 H/s.
- No qualifying hash was found, so no share candidate or submission occurred.
- The append-only log grew from 6 to 13 records.
- Native analysis reported 3 completed runs and no failures.
- The analyzer's weighted rate matched the live bounded-mining result.

This validation records no username, payout address, extra nonce value, job ID,
raw event record, or raw protocol data.

## Continuous Three-Chunk Live Validation

- Chunks completed: 3
- Hashes checked: 300000
- Elapsed mining time: 531117792 ns
- Weighted hash rate: 564846.45 H/s
- Jobs used: 1
- Job replacements: 0
- Candidate and submission: none
- One difficulty update arrived between chunks.
- The prepared job remained unchanged because no later `mining.notify` arrived.
- Aggregate log summary: 4 completed runs, 400000 hashes, and no failures.

This sanitized record omits job IDs, usernames, payout addresses, both extra
nonce values, raw events, and protocol payloads.
