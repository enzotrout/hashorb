# Milestone 0.3 — Python Environment

## Objective

Create a reproducible Python development environment for HashOrb.

## Completed

- Installed uv
- Created `.venv`
- Initialized `pyproject.toml`
- Generated `uv.lock`
- Installed pytest
- Installed Ruff
- Installed mypy
- Added environment validation tests
- Verified dependency lock
- Configured project tooling

## Validation

- Ruff passed
- mypy passed
- pytest passed (3 tests)
- uv lock verification passed

## Lessons Learned

The virtual environment is machine-specific and should never be committed.

Instead, the project stores:

- pyproject.toml
- uv.lock

These files completely describe the project's Python environment.

## Next Milestone

Milestone 0.4 — VS Code Professional Configuration

## Milestone 0.4 — VS Code Professional Setup (Partial)

Completed:

- Verified VS Code installation
- Verified Apple Silicon native build
- Opened HashOrb workspace
- Configured VS Code to use the project's Python virtual environment (`.venv`)
- Configured the project to standardize on Ruff for formatting and linting
- Removed Black Formatter and Pylint to avoid overlapping tooling
- Improved `.gitignore`
- Added shared `.vscode/settings.json`
- Simplified the VS Code Explorer by hiding generated directories

Notes:

- VS Code remembers previously selected Python interpreters. The workspace has been configured to always use `./.venv`.
- The integrated terminal now activates the project's virtual environment automatically.
- The project now has a consistent Python editing experience across contributors.
