# Linux Distribution Boundary

Hashsphere uses the shared Python package. Run `scripts/install-unix.sh` for a
user-local CPU installation. CUDA remains a separate explicit local source
build and is not a portable wheel. Full instructions are in
`docs/13-installation-and-packaging.md`.
